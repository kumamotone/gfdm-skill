<% provide(:title, "SKILL AVERAGE SP #{params[:from]} 〜 #{params[:to]} (DrumMania)") %>

<h1>
  <%= "SKILL AVERAGE SP #{params[:from]} 〜 #{params[:to]}" %> (DrumMania)
</h1>

<h2>Hot</h2>

<%= render partial: 'averagetable' %>