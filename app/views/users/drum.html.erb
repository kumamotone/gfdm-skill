<% provide(:title, @user.name + ' さんのスキル (DrumMania)' ) %>
  <h1>
    <%= @user.name %> さんのスキル (DrumMania) 
  </h1>
  <div class="well">
    <%= @user.d_comment %>
  </div>
  <%= render 'sptabledrum' %>
  <%= render 'skilltable' %>
